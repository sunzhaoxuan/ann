# One command-line argument per line. test.sh always compiles main.cc, and
# main.cc reads this file after qsub.sh copies files/ to the compute node.
--data-dir
/anndata
--method
flat
--queries
2000
--k
10
--output
files/workflow_results.csv
